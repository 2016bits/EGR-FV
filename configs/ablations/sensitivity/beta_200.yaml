base: ../../remix.yaml

experiment:
  name: sens_beta_200

data:
  routing_path: data/HOVER/routed/sensitivity/beta_200.jsonl

routing:
  beta_shortcut_gate: 2.0

checkpoints:
  remix: outputs/HOVER/checkpoints/sensitivity/beta_200/remix_best.pt

outputs:
  checkpoint_dir: outputs/HOVER/checkpoints/sensitivity/beta_200
  log_dir: outputs/HOVER/logs/sensitivity/beta_200
  prediction_dir: outputs/HOVER/predictions/sensitivity/beta_200

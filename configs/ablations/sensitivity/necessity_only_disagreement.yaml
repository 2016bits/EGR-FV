base: ../../remix.yaml

experiment:
  name: sens_necessity_only_disagreement

data:
  routing_path: data/HOVER/routed/sensitivity/necessity_only_disagreement.jsonl

checkpoints:
  remix: outputs/HOVER/checkpoints/sensitivity/necessity_only_disagreement/remix_best.pt

outputs:
  checkpoint_dir: outputs/HOVER/checkpoints/sensitivity/necessity_only_disagreement
  log_dir: outputs/HOVER/logs/sensitivity/necessity_only_disagreement
  prediction_dir: outputs/HOVER/predictions/sensitivity/necessity_only_disagreement

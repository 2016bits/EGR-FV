base: ../../remix.yaml

experiment:
  name: sens_necessity_only_grounded

data:
  routing_path: data/HOVER/routed/sensitivity/necessity_only_grounded.jsonl

checkpoints:
  remix: outputs/HOVER/checkpoints/sensitivity/necessity_only_grounded/remix_best.pt

outputs:
  checkpoint_dir: outputs/HOVER/checkpoints/sensitivity/necessity_only_grounded
  log_dir: outputs/HOVER/logs/sensitivity/necessity_only_grounded
  prediction_dir: outputs/HOVER/predictions/sensitivity/necessity_only_grounded

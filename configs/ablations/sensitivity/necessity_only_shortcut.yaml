base: ../../remix.yaml

experiment:
  name: sens_necessity_only_shortcut

data:
  routing_path: data/HOVER/routed/sensitivity/necessity_only_shortcut.jsonl

checkpoints:
  remix: outputs/HOVER/checkpoints/sensitivity/necessity_only_shortcut/remix_best.pt

outputs:
  checkpoint_dir: outputs/HOVER/checkpoints/sensitivity/necessity_only_shortcut
  log_dir: outputs/HOVER/logs/sensitivity/necessity_only_shortcut
  prediction_dir: outputs/HOVER/predictions/sensitivity/necessity_only_shortcut

base: fever_ablation_full_in_sample_routing.yaml

experiment:
  name: full_in_sample_routing_symmetric_fever

data:
  test_path: data/FEVER/converted_data/symmetric.json

outputs:
  prediction_dir: outputs/FEVER/predictions/full_in_sample_routing/symmetric_fever

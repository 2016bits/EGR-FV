base: fever.yaml

experiment:
  name: random_remix_only_fever
  mode: remix_only
  pseudo_group_strategy: random
  uniform_sample_weights: true

model:
  use_fusion: false

loss:
  use_grounded_dominant: false
  use_evidence_contrast: false
  lambda_shortcut: 0.1
  lambda_orth: 0.0

checkpoints:
  remix: outputs/FEVER/checkpoints/random_remix_only/remix_best.pt

outputs:
  checkpoint_dir: outputs/FEVER/checkpoints/random_remix_only
  log_dir: outputs/FEVER/logs/random_remix_only
  prediction_dir: outputs/FEVER/predictions/random_remix_only/fever

inference:
  branch: grounded

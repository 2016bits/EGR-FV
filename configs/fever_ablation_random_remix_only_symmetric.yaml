base: fever_ablation_random_remix_only.yaml

experiment:
  name: random_remix_only_symmetric_fever

data:
  test_path: data/FEVER/converted_data/symmetric.json

outputs:
  prediction_dir: outputs/FEVER/predictions/random_remix_only/symmetric_fever

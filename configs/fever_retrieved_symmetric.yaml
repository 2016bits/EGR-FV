base: fever_retrieved.yaml

experiment:
  name: full_egr_fv_fever_retrieved_symmetric

data:
  test_path: data/FEVER/retrieved/symmetric.jsonl

outputs:
  prediction_dir: outputs/FEVER/retrieved/predictions/symmetric
